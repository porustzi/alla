import type React from 'react';
import { Award, BookOpen, BadgeCheck, Briefcase, FlaskConical, Globe, GraduationCap, Trophy, Users } from 'lucide-react';

export const iconMap: Record<string, React.ElementType> = {
  Award,
  BookOpen,
  BadgeCheck,
  Briefcase,
  FlaskConical,
  Globe,
  GraduationCap,
  Trophy,
  Users,
};
